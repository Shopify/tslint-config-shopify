import Foo from './foo';
import Bar from './Bar';

import {Foo, Bar, Baz} from 'FooBarBaz';
                            ~~~~~~~~~~~~ [Imports should be listed in the following order: module imports, absolute imports, relative imports.]
import BarModule from 'BarModule';
                      ~~~~~~~~~~~~ [Imports should be listed in the following order: module imports, absolute imports, relative imports.]

import Foo2 from './foo2';
import Bar2 from './Bar2';
