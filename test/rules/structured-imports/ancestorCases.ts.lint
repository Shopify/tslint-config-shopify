import * as relative from "./relativeImport1";
import { relativeImport } from "./relativeImport2";
import { anotherRelativeImport } from "../ancestorImport";
                                      ~~~~~~~~~~~~~~~~~~~~ [0]

import AnotherAbsolute from 'another-absolute';
                            ~~~~~~~~~~~~~~~~~~~ [0]
import * as SomethingAbsolute from 'something-absolute';
                                   ~~~~~~~~~~~~~~~~~~~~~ [0]
import {absolute1, absolute2} from 'library';
                                   ~~~~~~~~~~ [0]

[0]: Imports should be listed in the following order: module imports, absolute imports, ancestor imports, sibling imports.
